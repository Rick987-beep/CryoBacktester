# analysis/ (retired)

User research I/O moved into the private workspace submodule:

- **Active:** `workspace/analysis/` (`run_audit/`, `livecompare/`)
- **Closed:** `workspace/archive/analysis/`

Do not add new artefacts here. Product CLIs write under `workspace/analysis/`.
