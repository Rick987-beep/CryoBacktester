# Product archive

Product graveyard for the public backtester package — **not** the user-research
archive (`workspace/archive/`).

| Path | Contents |
|------|----------|
| `strategies_to_be_fixed/` | Legacy unfinished strategy ports (not in CLI registry) |
| `planning/` | Old specs, handover notes, parked UI wishlist |
| `data_archive` / `data_spot_rebuilt` | Symlinks into `data/archive/` (contents gitignored) |

Code and planning docs may be tracked. Large data symlink targets stay ignored.
User research (old analysis trees, closed grids) belongs in `workspace/archive/`.
Marketing retired ships: `workspace/marketing/archive/`.
